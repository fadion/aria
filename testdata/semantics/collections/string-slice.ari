// Slicing a string follows the rune-indexing rule the subscript already uses,
// so a string with accents slices the way it reads.
let s = "héllo"
println(s[1..3])
println(s[0..0])
println(s[-2..-1])
println(s[3..1])
println(s[9..12])
println(""[0..2])
